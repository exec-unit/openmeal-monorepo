# ============================================================================
# OpenMeal Backend - Core Application Microservices
# ============================================================================
# This file contains definitions for all custom microservices.
# Infrastructure services (postgres, redis, nginx, etc.) are located in:
#   - compose/infra.yml (core infrastructure)
#   - compose/monitoring.yml (monitoring stack)
#
# Usage:
#   Set ENVIRONMENT in .env.infra, then:
#   make up      - start services
#   make down    - stop services
#   make restart - restart services
# ============================================================================

# Shared network for all services
networks:
  default:
    name: ${DOCKER_NETWORK_NAME:-openmeal_net}
    external: true

# Volumes for microservices will be added as services are developed
# volumes:
#   service_data:

# ============================================================================
# IMPORTANT: Uncomment and configure services as you develop them!
# ============================================================================
# services:
#   api-gateway:
#     image: cr.yandex/${REGISTRY_ID}/api-gateway:${API_GATEWAY_VERSION:-latest}
#     env_file:
#       - .env.api-gateway
#     container_name: ${CONTAINER_PREFIX}-api-gateway
#     restart: unless-stopped
#     ports:
#       - "${SERVER_PORT:-8080}:8080"
#     networks:
#       - default

#   user-service:
#     image: cr.yandex/${REGISTRY_ID}/user-service:${USER_SERVICE_VERSION:-latest}
#     env_file:
#       - .env.user-service
#     container_name: ${CONTAINER_PREFIX}-user-service
#     restart: unless-stopped
#     ports:
#       - "${SERVER_PORT:-8081}:8081"
#     networks:
#       - default
