---
# ============================================================================
# Docker Role - Handlers
# ============================================================================

- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: yes
  when: not mock_systemd | default(false)
