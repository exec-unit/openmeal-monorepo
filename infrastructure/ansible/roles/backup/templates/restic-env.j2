# Restic environment configuration
# This file is sourced by backup scripts

export RESTIC_REPOSITORY="{{ backup_restic_repository }}"
export RESTIC_PASSWORD="{{ vault_restic_password }}"
export AWS_ACCESS_KEY_ID="{{ vault_s3_access_key_id }}"
export AWS_SECRET_ACCESS_KEY="{{ vault_s3_secret_access_key }}"
export AWS_DEFAULT_REGION="{{ backup_s3_region }}"
