collections:
  # Provides core modules for Docker orchestration (docker_container, docker_compose_v2).
  - name: community.docker
    version: "5.0.1"

  # General purpose modules for system administration tasks.
  - name: community.general
    version: "12.0.1"
